name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: No-op until real code lands
        run: echo "CI skeleton in place - add real build/test steps as each layer gets code."
